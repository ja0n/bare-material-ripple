bare-material-ripple
======

A bare material ripple implementation plus MutationObserver.

## Install
```sh
npm install bare-material-ripple
```

## Usage
```js
// See example.html
$.ripple.init();
$.ripple.attach(domEl);
// init(), attach(), create() and observe() available
```
